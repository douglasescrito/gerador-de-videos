export async function executar(contexto) {
  const {

  } = contexto;
  {
    throw new Error("refine por interactionId não possui contrato cookie-only verificável. Use generate --task edit --video <mp4> com a sessão do Gerenciador de Credenciais.");
  }
}
